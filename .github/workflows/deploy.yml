name: Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy-backend:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: sudo docker compose down
      - run: sudo docker compose up -d --build
      - run: sudo docker ps